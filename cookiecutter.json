{
    "project_name": "Service Framework",
    "base_name": "{{ cookiecutter.project_name.split()|join(' ')|lower|replace(' ', '_')|replace('-', '_') }}",
    "app_name": "{{ cookiecutter.base_name|replace('_', '-') }}",
    "description": "A framework that can be used as a base to build service on Rehive",
    "module_name": "{{ cookiecutter.base_name }}",
    "github_repository_name": "{{ cookiecutter.base_name |replace('_', '-') }}",
    "server_infrastructure": [
        "Heroku + TravisCI",
        "GCloud + Kubernetes"
    ],
    "_copy_without_render": [
        "*.html"
    ]
}